# Contributing to Faber-Aeternus

1. Keep it safe (`#![forbid(unsafe_code)]` unless utterly necessary).
2. Adhere to Rust 2024 edition patterns.
3. Every commit must pass `cargo fmt` and `cargo clippy --all-features`.
4. Submit PRs against `main`. Do not break Bubblewrap fallback!
